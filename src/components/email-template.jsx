import * as React from "react";

export const EmailTemplate = ({ firstName, pro, emailaddress }) => (
  <div>
    <h1>name, {firstName}!</h1>
    <h1>profission, {pro}!</h1>
    <h1>email addres, {emailaddress}!</h1>
  </div>
  
);
